const Title = ({title, age, name}) => {
    return (
        <div style={{color:'blue'}}>
            {title}
        </div>
    )
}

export default Title;

const sum = (a,b) => {
    return(a+b)
}
console.log(sum(5,6));